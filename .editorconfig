root = true

[*]
charset = utf-8
end_of_line = lf
insert_final_newline = true
trim_trailing_whitespace = true
indent_style = space
indent_size = 4

[*.md]
max_line_length = 80
# Trailing spaces are a hard line break in markdown, so don't strip them.
trim_trailing_whitespace = false
