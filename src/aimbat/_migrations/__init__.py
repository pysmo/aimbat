"""Alembic migration environment package for AIMBAT.

Serves as Alembic's `script_location` (see `alembic.ini`). Contains
`env.py`, which resolves the database connection to run migrations against,
and the `versions/` directory of revision scripts.
"""
